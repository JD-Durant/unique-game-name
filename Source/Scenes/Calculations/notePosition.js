/*import "phaser";
export class notePosition extends Phaser.Scene {constructor(){super("notes")}
  recordNote(note) {
    var test = note.y;
    console.log(test);
  };

  update(){
    this.recordNote(note1);
  }
}*/